// Placez vos paramètres dans ce fichier pour remplacer les paramètres par défaut et les paramètres utilisateur.
{
    // save files when the focus moves out of the VS Code window
    "files.autoSave": "onWindowChange"
}